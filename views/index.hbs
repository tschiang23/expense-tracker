<div class='container d-flex flex-column align-items-center mt-5'>
  <h1>支出紀錄</h1>
  <h3>總金額 </h3>
  <h2>{{totalAmount}}</h2>
  <ul>
    {{#each records}}
      <li>
        <i class='{{this.icon}}'></i>
        {{this.name}}
        {{this.date}}
        {{this.amount}}
        <a class='btn btn-secondary' href='/records/{{this._id}}/edit'>編輯</a>
      </li>

    {{/each}}
  </ul>

  <div><a class='btn btn-primary' href='/records/new'>新增支出</a></div>
</div>